import createTheme from '@hurumap-ui/charts/styles/createTheme';

export default createTheme({
  chart: {
    axis: {
      labelWidth: 150
    },
    bar: {
      width: 350,
      height: 350,
      barWidth: 30,
      offset: 60
    }
  }
});
